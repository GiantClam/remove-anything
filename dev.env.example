# 本地开发环境配置示例
# 复制到 .env.local 使用

NODE_ENV=development
DATABASE_URL=file:./dev.db
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=development-secret-key

# 开发模式自动登录（设置为true启用）
DEV_AUTO_LOGIN=true

# Google OAuth（本地开发可以使用占位符）
GOOGLE_CLIENT_ID=google-client-id-placeholder
GOOGLE_CLIENT_SECRET=google-client-secret-placeholder

# 其他可选配置可以留空或使用占位符
REPLICATE_API_TOKEN=replicate-token-placeholder
GEMINI_API_KEY=gemini-key-placeholder

# 使用说明：
# 1. 复制此文件到 .env.local
# 2. DEV_AUTO_LOGIN=true 将自动启用开发模式登录
# 3. 无需配置真实的 Google OAuth，系统会自动使用模拟用户
